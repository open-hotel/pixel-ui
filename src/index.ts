import Vue from 'vue'
import { WindowManager, Window, Button } from './components'

Vue.component('px-window-manager', WindowManager)
Vue.component('px-window', Window)
Vue.component('px-btn', Button)
